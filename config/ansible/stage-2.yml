- name: DevOps Linux Stage 2
  hosts: all
  gather_facts: true
  strategy: free
  tasks:
    - name: Print ansible facts
      debug:
        var: ansible_facts
        verbosity: 2
    - name: Print setup config
      debug:
        var: setup_facts
        verbosity: 2
